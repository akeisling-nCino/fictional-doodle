[package]
name = "git-tidy"
version = "0.3.0"
edition = "2024"

[dependencies]
clap = { version = "4.5.60", features = ["derive"] }
colored = "3.1.1"

[workspace.lints.clippy]
unwrap_used = "warn"
expect_used = "warn"
